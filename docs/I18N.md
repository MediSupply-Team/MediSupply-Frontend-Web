# Sistema de Internacionalización (i18n)

## 🌍 Configuración

El proyecto usa **next-intl** para gestionar traducciones, formatos de fecha, moneda y diferencias regionales.

## 📦 Idiomas y Regiones Soportados

### Español
- `es-CO` - Español (Colombia) - **Por defecto**
- `es-MX` - Español (México)
- `es-AR` - Español (Argentina)
- `es-PE` - Español (Perú)
- `es-CL` - Español (Chile)

### English
- `en-US` - English (United States)
- `en-GB` - English (United Kingdom)

## 🎯 Características

### 1. **Traducciones Automáticas**
Todos los textos de la interfaz se traducen automáticamente según el idioma seleccionado.

### 2. **Variantes Regionales**
Palabras que varían según el país:
- **Bodega**: "Bodega" (CO), "Almacén" (MX), "Depósito" (AR)
- **Celular**: "Celular" (Latam), "Mobile" (US), "Mobile" (UK)
- **Computador**: "Computador" (CO), "Computadora" (MX/AR), "Computer" (EN)

### 3. **Formatos Automáticos**

#### Moneda
- Colombia: COP ($ 1.234.567,89)
- México: MXN ($ 1,234,567.89)
- Argentina: ARS ($ 1.234.567,89)
- Estados Unidos: USD ($1,234,567.89)

#### Fecha y Hora
- Formato automático según la región
- Zona horaria específica por país
- Fechas relativas ("hace 2 horas", "2 hours ago")

#### Números
- Separadores decimales según región
- Formatos de miles según convención local

## 🚀 Uso en Componentes

### Hook `useI18n`

```tsx
'use client';

import { useI18n } from '@/hooks/useI18n';

export default function MiComponente() {
  const { t, formatCurrency, formatDate, formatNumber } = useI18n();

  return (
    <div>
      {/* Traducción simple */}
      <h1>{t('products.title')}</h1>
      
      {/* Formateo de moneda */}
      <p>{formatCurrency(1234567.89)}</p>
      
      {/* Formateo de fecha */}
      <p>{formatDate(new Date())}</p>
      
      {/* Formateo de números */}
      <p>{formatNumber(1234567.89)}</p>
    </div>
  );
}
```

### Funciones Disponibles

#### `t(key: string)`
Obtiene la traducción para una clave específica.

```tsx
t('common.save') // "Guardar" o "Save"
t('products.title') // "Productos" o "Products"
```

#### `formatCurrency(amount: number)`
Formatea un número como moneda según la región.

```tsx
formatCurrency(1000) 
// CO: "$ 1.000,00"
// MX: "$ 1,000.00"
// US: "$1,000.00"
```

#### `formatDate(date: Date | string, options?)`
Formatea una fecha según la región y zona horaria.

```tsx
formatDate(new Date(), { 
  dateStyle: 'full',
  timeStyle: 'short' 
})
// CO: "miércoles, 9 de noviembre de 2025, 14:30"
// US: "Wednesday, November 9, 2025 at 2:30 PM"
```

#### `formatNumber(number: number, options?)`
Formatea un número según la región.

```tsx
formatNumber(1234567.89)
// CO: "1.234.567,89"
// MX: "1,234,567.89"
```

#### `formatRelativeTime(date: Date | string)`
Muestra tiempo relativo desde una fecha.

```tsx
formatRelativeTime(new Date(Date.now() - 2 * 60 * 60 * 1000))
// ES: "hace 2 horas"
// EN: "2 hours ago"
```

## 📝 Agregar Nuevas Traducciones

### 1. Editar archivos de mensajes

Ubicación: `messages/{locale}.json`

```json
{
  "mySection": {
    "title": "Mi Título",
    "description": "Mi descripción"
  }
}
```

### 2. Usar en componente

```tsx
const { t } = useI18n();

<h1>{t('mySection.title')}</h1>
<p>{t('mySection.description')}</p>
```

## 🔧 Cambiar Idioma/Región

El selector de idioma está disponible en el header de la aplicación (ícono de globo).

### Programáticamente

```tsx
// Guardar en cookie y recargar
document.cookie = `locale=es-MX; path=/; max-age=31536000`;
window.location.reload();
```

## 📂 Estructura de Archivos

```
src/
├── i18n/
│   ├── config.ts          # Configuración de locales
│   └── request.ts         # Configuración de next-intl
├── hooks/
│   └── useI18n.ts         # Hook personalizado
└── components/
    ├── LocaleSelector.tsx # Selector de idioma
    └── providers/
        └── I18nProvider.tsx

messages/
├── es-CO.json
├── es-MX.json
├── es-AR.json
├── es-PE.json
├── es-CL.json
├── en-US.json
└── en-GB.json
```

## 🎨 Configuración Regional Automática

Cada locale tiene configuración automática de:

| Locale | Moneda | Zona Horaria | Separador Decimal |
|--------|--------|--------------|-------------------|
| es-CO  | COP    | America/Bogota | , (coma) |
| es-MX  | MXN    | America/Mexico_City | . (punto) |
| es-AR  | ARS    | America/Argentina/Buenos_Aires | , (coma) |
| es-PE  | PEN    | America/Lima | , (coma) |
| es-CL  | CLP    | America/Santiago | , (coma) |
| en-US  | USD    | America/New_York | . (punto) |
| en-GB  | GBP    | Europe/London | . (punto) |

## 🌟 Mejores Prácticas

1. **Siempre usar el hook `useI18n`** en lugar de hardcodear textos
2. **Usar formateadores** para monedas, fechas y números
3. **Agrupar traducciones** por módulo/sección
4. **Mantener consistencia** en las claves de traducción
5. **Probar en múltiples locales** antes de desplegar

## 🔄 Actualizaciones en Tiempo Real

Los cambios de idioma/región se aplican inmediatamente sin necesidad de cerrar sesión. El sistema recarga la página automáticamente para aplicar las nuevas configuraciones.
